/*
 * sniperfix.inc — placeholder for the lost sniper-fix include.
 * The original hooked shot processing to fix sniper-rifle hit registration;
 * the gamemode references no symbol from it, so an empty include keeps the
 * build intact. Re-add a real fix here if desired.
 */
#if defined _sniperfix_included
	#endinput
#endif
#define _sniperfix_included
